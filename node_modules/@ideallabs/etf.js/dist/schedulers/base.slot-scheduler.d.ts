export type GenerateParams<T extends {}> = {
    slotAmount: number;
    currentSlot: number;
} & T;
export declare abstract class SlotScheduler<T extends {}> {
    abstract generateSchedule(params: GenerateParams<T>): number[];
}
